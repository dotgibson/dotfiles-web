{
  "name": "dotfiles-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Showcase site for the dotfiles ecosystem (Core -> OS-native -> Role).",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "metrics": "node scripts/collect-metrics.mjs",
    "snippets": "node scripts/collect-snippets.mjs",
    "corpus": "node scripts/collect-corpus.mjs",
    "coverage": "node scripts/collect-coverage.mjs",
    "data": "npm run metrics && npm run snippets && npm run corpus && npm run coverage",
    "data:strict": "node scripts/collect-metrics.mjs --strict && npm run snippets && node scripts/collect-corpus.mjs --strict && node scripts/collect-coverage.mjs --strict",
    "verify:flags": "node scripts/verify-bootstrap-flags.mjs",
    "check": "astro check",
    "astro": "astro"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/node": "^22.20.0",
    "astro": "^7.0.7",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.1",
    "@xterm/addon-fit": "0.11.0",
    "@xterm/xterm": "6.0.0"
  }
}
